---
---
<div class="trust">
  <p class="quote"><!-- PLACEHOLDER: voice-guide pass — see spec §8 trust line --></p>
  <a href="/security" class="trust-link">Read the security model →</a>
</div>
<style>
  .trust { max-width: 560px; margin: 0 auto; text-align: center; padding: var(--space-5) var(--space-4); }
  .quote { font-family: var(--font-display); font-size: 22px; font-style: italic; line-height: 1.4; font-weight: 600; color: var(--ink-primary); position: relative; min-height: 1.4em; }
  .quote::before, .quote::after { color: var(--copper-deep); }
  .quote::before { content: '"'; }
  .quote::after { content: '"'; }
  .trust-link { font-size: 12px; text-transform: uppercase; letter-spacing: 0.08em; color: var(--ink-secondary); font-weight: 600; text-decoration: none; }
  .trust-link:hover { color: var(--ink-primary); }
</style>
