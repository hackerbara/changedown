/**
 * Re-export wrapper — canonical implementation lives in changetracks.
 */
export { executeCommand, ParseError } from 'changetracks/schema-executor';
export type { ToolHandler, CommandDef } from 'changetracks/schema-executor';
