# ChangeDown — Change Tracking for AI Agents

For tracked markdown files use the ChangeDown MCP tools (propose_change, read_tracked_file, propose_batch, review_changes), not raw Edit/Write. Which files are tracked: see .changedown/config.toml. Full instructions: .cursor/skills/changedown/SKILL.md
