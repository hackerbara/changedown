/**
 * Re-export wrapper — canonical implementation lives in changedown.
 */
export { executeCommand, ParseError } from 'changedown/schema-executor';
export type { ToolHandler, CommandDef } from 'changedown/schema-executor';
